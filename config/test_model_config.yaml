defaults:
  - problem: mazes
  - override hydra/job_logging: custom
  - _self_

hydra:
  run:
    dir: ./outputs/${name}/testing-${run_id}
  job_logging:
    handlers:
      file:
        filename: testing.log

name: testing_default

problem:
  model:
    test_iterations:
      low: 40
      high: 50
    # model_path: "/home/gabriel/Lydia/dpth_copy2/outputs/training_default/training-homeless-Sok" 
    model_path: "/home/gabriel/Lydia/dpth_copy2/outputs/training_default/training-unmeet-Kearstin"

quick_test: True
termination_condition: True
time_evaluation: False
accuracy_evaluation: False

